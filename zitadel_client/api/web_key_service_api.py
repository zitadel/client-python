# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from typing import Any, Dict, List, Optional
from pydantic import StrictBool, StrictFloat, StrictInt, StrictStr

from zitadel_client.models.web_key_service_activate_web_key_request import (
    WebKeyServiceActivateWebKeyRequest,
)
from zitadel_client.models.web_key_service_activate_web_key_response import (
    WebKeyServiceActivateWebKeyResponse,
)
from zitadel_client.models.web_key_service_connect_error import (
    WebKeyServiceConnectError,
)
from zitadel_client.models.web_key_service_create_web_key_request import (
    WebKeyServiceCreateWebKeyRequest,
)
from zitadel_client.models.web_key_service_create_web_key_response import (
    WebKeyServiceCreateWebKeyResponse,
)
from zitadel_client.models.web_key_service_delete_web_key_request import (
    WebKeyServiceDeleteWebKeyRequest,
)
from zitadel_client.models.web_key_service_delete_web_key_response import (
    WebKeyServiceDeleteWebKeyResponse,
)
from zitadel_client.models.web_key_service_list_web_keys_response import (
    WebKeyServiceListWebKeysResponse,
)

from ..api_client import ApiClient
from ..api_result import ApiResult
from ..configuration import Configuration
from .base_api import BaseApi
from ..value_serializer import ValueSerializer
from ..auth.authenticator import Authenticator
from ..errors import ApiException


class WebKeyServiceApi(BaseApi):
    """WebKeyServiceApi provides methods for the WebKeyService API group."""

    def __init__(
        self,
        api_client: Optional[ApiClient] = None,
        config: Optional[Configuration] = None,
        authenticator: Optional[Authenticator] = None,
    ):
        super().__init__(api_client, config, authenticator)

    async def activate_web_key(
        self,
        web_key_service_activate_web_key_request: WebKeyServiceActivateWebKeyRequest,
    ) -> WebKeyServiceActivateWebKeyResponse:
        """Activate Web Key
        Switch the active signing web key. The previously active key will be deactivated.  Note that the JWKs OIDC endpoint returns a cacheable response.  Therefore it is not advised to activate a key that has been created within the cache duration (default is 5min),  as the public key may not have been propagated to caches and clients yet.   Required permission:    - `iam.web_key.write`
        :param web_key_service_activate_web_key_request:  (required)

        :return: WebKeyServiceActivateWebKeyResponse
        :raises ApiException: if fails to make API call
        """
        if web_key_service_activate_web_key_request is None:
            raise ValueError(
                "Missing the required parameter 'web_key_service_activate_web_key_request'"
            )

        result = await self.activate_web_key_with_http_info(
            web_key_service_activate_web_key_request
        )

        if result.data is None:
            # This operation declares a non-void return type, so an empty /
            # undecodable response body is a contract violation. Raise the
            # SDK's typed ApiException (rather than an assert that vanishes
            # under -O, or a silent None) so callers get one catchable error.
            raise ApiException(
                status_code=result.status_code,
                message="Expected a response body but the server returned none",
                response_body=result.raw_body,
                response_headers=result.headers,
            )
        return result.data

    async def activate_web_key_with_http_info(
        self,
        web_key_service_activate_web_key_request: WebKeyServiceActivateWebKeyRequest,
    ) -> "ApiResult[WebKeyServiceActivateWebKeyResponse]":
        """Activate Web Key (with HTTP info)
        Switch the active signing web key. The previously active key will be deactivated.  Note that the JWKs OIDC endpoint returns a cacheable response.  Therefore it is not advised to activate a key that has been created within the cache duration (default is 5min),  as the public key may not have been propagated to caches and clients yet.   Required permission:    - `iam.web_key.write`
        :param web_key_service_activate_web_key_request:  (required)

        :return: ApiResult containing the response data, status code, raw body, and headers
        :raises ApiException: if fails to make API call
        """
        if web_key_service_activate_web_key_request is None:
            raise ValueError(
                "Missing the required parameter 'web_key_service_activate_web_key_request'"
            )

        path = "/zitadel.webkey.v2.WebKeyService/ActivateWebKey"
        query_params: Dict[str, Any] = {}
        header_params: Dict[str, str] = {}
        body = web_key_service_activate_web_key_request

        return await self._invoke_api_for_result(
            "POST",
            path,
            query_params,
            header_params,
            body,
            ["application/json"],
            "application/json",
            "WebKeyServiceActivateWebKeyResponse",
            None,
        )

    async def create_web_key(
        self,
        web_key_service_create_web_key_request: WebKeyServiceCreateWebKeyRequest,
    ) -> WebKeyServiceCreateWebKeyResponse:
        """Create Web Key
        Generate a private and public key pair. The private key can be used to sign OIDC tokens after activation.  The public key can be used to validate OIDC tokens.  The newly created key will have the state `STATE_INITIAL` and is published to the public key endpoint.  Note that the JWKs OIDC endpoint returns a cacheable response.   If no key type is provided, a RSA key pair with 2048 bits and SHA256 hashing will be created.   Required permission:    - `iam.web_key.write`
        :param web_key_service_create_web_key_request:  (required)

        :return: WebKeyServiceCreateWebKeyResponse
        :raises ApiException: if fails to make API call
        """
        if web_key_service_create_web_key_request is None:
            raise ValueError(
                "Missing the required parameter 'web_key_service_create_web_key_request'"
            )

        result = await self.create_web_key_with_http_info(
            web_key_service_create_web_key_request
        )

        if result.data is None:
            # This operation declares a non-void return type, so an empty /
            # undecodable response body is a contract violation. Raise the
            # SDK's typed ApiException (rather than an assert that vanishes
            # under -O, or a silent None) so callers get one catchable error.
            raise ApiException(
                status_code=result.status_code,
                message="Expected a response body but the server returned none",
                response_body=result.raw_body,
                response_headers=result.headers,
            )
        return result.data

    async def create_web_key_with_http_info(
        self,
        web_key_service_create_web_key_request: WebKeyServiceCreateWebKeyRequest,
    ) -> "ApiResult[WebKeyServiceCreateWebKeyResponse]":
        """Create Web Key (with HTTP info)
        Generate a private and public key pair. The private key can be used to sign OIDC tokens after activation.  The public key can be used to validate OIDC tokens.  The newly created key will have the state `STATE_INITIAL` and is published to the public key endpoint.  Note that the JWKs OIDC endpoint returns a cacheable response.   If no key type is provided, a RSA key pair with 2048 bits and SHA256 hashing will be created.   Required permission:    - `iam.web_key.write`
        :param web_key_service_create_web_key_request:  (required)

        :return: ApiResult containing the response data, status code, raw body, and headers
        :raises ApiException: if fails to make API call
        """
        if web_key_service_create_web_key_request is None:
            raise ValueError(
                "Missing the required parameter 'web_key_service_create_web_key_request'"
            )

        path = "/zitadel.webkey.v2.WebKeyService/CreateWebKey"
        query_params: Dict[str, Any] = {}
        header_params: Dict[str, str] = {}
        body = web_key_service_create_web_key_request

        return await self._invoke_api_for_result(
            "POST",
            path,
            query_params,
            header_params,
            body,
            ["application/json"],
            "application/json",
            "WebKeyServiceCreateWebKeyResponse",
            None,
        )

    async def delete_web_key(
        self,
        web_key_service_delete_web_key_request: WebKeyServiceDeleteWebKeyRequest,
    ) -> WebKeyServiceDeleteWebKeyResponse:
        """Delete Web Key
        Delete a web key pair. Only inactive keys can be deleted. Once a key is deleted,  any tokens signed by this key will be invalid.  Note that the JWKs OIDC endpoint returns a cacheable response.  In case the web key is not found, the request will return a successful response as  the desired state is already achieved.  You can check the change date in the response to verify if the web key was deleted during the request.   Required permission:    - `iam.web_key.delete`
        :param web_key_service_delete_web_key_request:  (required)

        :return: WebKeyServiceDeleteWebKeyResponse
        :raises ApiException: if fails to make API call
        """
        if web_key_service_delete_web_key_request is None:
            raise ValueError(
                "Missing the required parameter 'web_key_service_delete_web_key_request'"
            )

        result = await self.delete_web_key_with_http_info(
            web_key_service_delete_web_key_request
        )

        if result.data is None:
            # This operation declares a non-void return type, so an empty /
            # undecodable response body is a contract violation. Raise the
            # SDK's typed ApiException (rather than an assert that vanishes
            # under -O, or a silent None) so callers get one catchable error.
            raise ApiException(
                status_code=result.status_code,
                message="Expected a response body but the server returned none",
                response_body=result.raw_body,
                response_headers=result.headers,
            )
        return result.data

    async def delete_web_key_with_http_info(
        self,
        web_key_service_delete_web_key_request: WebKeyServiceDeleteWebKeyRequest,
    ) -> "ApiResult[WebKeyServiceDeleteWebKeyResponse]":
        """Delete Web Key (with HTTP info)
        Delete a web key pair. Only inactive keys can be deleted. Once a key is deleted,  any tokens signed by this key will be invalid.  Note that the JWKs OIDC endpoint returns a cacheable response.  In case the web key is not found, the request will return a successful response as  the desired state is already achieved.  You can check the change date in the response to verify if the web key was deleted during the request.   Required permission:    - `iam.web_key.delete`
        :param web_key_service_delete_web_key_request:  (required)

        :return: ApiResult containing the response data, status code, raw body, and headers
        :raises ApiException: if fails to make API call
        """
        if web_key_service_delete_web_key_request is None:
            raise ValueError(
                "Missing the required parameter 'web_key_service_delete_web_key_request'"
            )

        path = "/zitadel.webkey.v2.WebKeyService/DeleteWebKey"
        query_params: Dict[str, Any] = {}
        header_params: Dict[str, str] = {}
        body = web_key_service_delete_web_key_request

        return await self._invoke_api_for_result(
            "POST",
            path,
            query_params,
            header_params,
            body,
            ["application/json"],
            "application/json",
            "WebKeyServiceDeleteWebKeyResponse",
            None,
        )

    async def list_web_keys(
        self,
        body: object,
    ) -> WebKeyServiceListWebKeysResponse:
        """List Web Keys
        List all web keys and their states.   Required permission:    - `iam.web_key.read`
        :param body:  (required)

        :return: WebKeyServiceListWebKeysResponse
        :raises ApiException: if fails to make API call
        """
        if body is None:
            raise ValueError("Missing the required parameter 'body'")

        result = await self.list_web_keys_with_http_info(body)

        if result.data is None:
            # This operation declares a non-void return type, so an empty /
            # undecodable response body is a contract violation. Raise the
            # SDK's typed ApiException (rather than an assert that vanishes
            # under -O, or a silent None) so callers get one catchable error.
            raise ApiException(
                status_code=result.status_code,
                message="Expected a response body but the server returned none",
                response_body=result.raw_body,
                response_headers=result.headers,
            )
        return result.data

    async def list_web_keys_with_http_info(
        self,
        body: object,
    ) -> "ApiResult[WebKeyServiceListWebKeysResponse]":
        """List Web Keys (with HTTP info)
        List all web keys and their states.   Required permission:    - `iam.web_key.read`
        :param body:  (required)

        :return: ApiResult containing the response data, status code, raw body, and headers
        :raises ApiException: if fails to make API call
        """
        if body is None:
            raise ValueError("Missing the required parameter 'body'")

        path = "/zitadel.webkey.v2.WebKeyService/ListWebKeys"
        query_params: Dict[str, Any] = {}
        header_params: Dict[str, str] = {}
        body = body

        return await self._invoke_api_for_result(
            "POST",
            path,
            query_params,
            header_params,
            body,
            ["application/json"],
            "application/json",
            "WebKeyServiceListWebKeysResponse",
            None,
        )
