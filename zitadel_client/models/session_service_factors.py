# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class SessionServiceFactors(BaseModel):
    """
    SessionServiceFactors
    """

    user: Optional[SessionServiceUserFactor] = Field(default=None, alias="user")
    password: Optional[SessionServicePasswordFactor] = Field(
        default=None, alias="password"
    )
    web_auth_n: Optional[SessionServiceWebAuthNFactor] = Field(
        default=None, alias="webAuthN"
    )
    intent: Optional[SessionServiceIntentFactor] = Field(default=None, alias="intent")
    totp: Optional[SessionServiceTOTPFactor] = Field(default=None, alias="totp")
    otp_sms: Optional[SessionServiceOTPFactor] = Field(default=None, alias="otpSms")
    otp_email: Optional[SessionServiceOTPFactor] = Field(default=None, alias="otpEmail")
    recovery_code: Optional[SessionServiceRecoveryCodeFactor] = Field(
        default=None, alias="recoveryCode"
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from zitadel_client.models.session_service_intent_factor import (
    SessionServiceIntentFactor,
)
from zitadel_client.models.session_service_otp_factor import SessionServiceOTPFactor
from zitadel_client.models.session_service_password_factor import (
    SessionServicePasswordFactor,
)
from zitadel_client.models.session_service_recovery_code_factor import (
    SessionServiceRecoveryCodeFactor,
)
from zitadel_client.models.session_service_totp_factor import SessionServiceTOTPFactor
from zitadel_client.models.session_service_user_factor import SessionServiceUserFactor
from zitadel_client.models.session_service_web_auth_n_factor import (
    SessionServiceWebAuthNFactor,
)

SessionServiceFactors.model_rebuild(raise_errors=False)
