# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field
from pydantic import StrictBool, StrictStr
from typing import List, Optional


class IdentityProviderServiceGenericOIDCConfig(BaseModel):
    """
    IdentityProviderServiceGenericOIDCConfig
    """

    issuer: Optional[StrictStr] = Field(
        default=None,
        alias="issuer",
        description="The OIDC issuer of the identity provider.",
    )
    client_id: Optional[StrictStr] = Field(
        default=None,
        alias="clientId",
        description="Client id generated by the identity provider.",
    )
    scopes: Optional[List[StrictStr]] = Field(
        default=None,
        alias="scopes",
        description="The scopes requested by ZITADEL during the request on the identity  provider.",
    )
    is_id_token_mapping: Optional[StrictBool] = Field(
        default=None,
        alias="isIdTokenMapping",
        description="If true, provider information get mapped from the id token, not from the  userinfo endpoint.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


IdentityProviderServiceGenericOIDCConfig.model_rebuild(raise_errors=False)
