# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class IdentityProviderServiceOptions(BaseModel):
    """
    IdentityProviderServiceOptions
    """

    is_linking_allowed: Optional[StrictBool] = Field(
        default=None,
        alias="isLinkingAllowed",
        description="Enable if users should be able to link an existing ZITADEL user with an  external account.",
    )
    is_creation_allowed: Optional[StrictBool] = Field(
        default=None,
        alias="isCreationAllowed",
        description="Enable if users should be able to create a new account in ZITADEL when  using an external account.",
    )
    is_auto_creation: Optional[StrictBool] = Field(
        default=None,
        alias="isAutoCreation",
        description="Enable if a new account in ZITADEL should be created automatically when  login with an external account.",
    )
    is_auto_update: Optional[StrictBool] = Field(
        default=None,
        alias="isAutoUpdate",
        description="Enable if a the ZITADEL account fields should be updated automatically on  each login.",
    )
    auto_linking: Optional[IdentityProviderServiceAutoLinkingOption] = Field(
        default=None, alias="autoLinking"
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from zitadel_client.models.identity_provider_service_auto_linking_option import (
    IdentityProviderServiceAutoLinkingOption,
)

IdentityProviderServiceOptions.model_rebuild(raise_errors=False)
