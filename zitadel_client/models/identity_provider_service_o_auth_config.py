# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class IdentityProviderServiceOAuthConfig(BaseModel):
    """
    IdentityProviderServiceOAuthConfig
    """

    client_id: Optional[StrictStr] = Field(
        default=None,
        alias="clientId",
        description="Client id generated by the identity provider.",
    )
    authorization_endpoint: Optional[StrictStr] = Field(
        default=None,
        alias="authorizationEndpoint",
        description="The endpoint where ZITADEL send the user to authenticate.",
    )
    token_endpoint: Optional[StrictStr] = Field(
        default=None,
        alias="tokenEndpoint",
        description="The endpoint where ZITADEL can get the token.",
    )
    user_endpoint: Optional[StrictStr] = Field(
        default=None,
        alias="userEndpoint",
        description="The endpoint where ZITADEL can get the user information.",
    )
    scopes: Optional[List[StrictStr]] = Field(
        default=None,
        alias="scopes",
        description="The scopes requested by ZITADEL during the request on the identity  provider.",
    )
    id_attribute: Optional[StrictStr] = Field(
        default=None,
        alias="idAttribute",
        description="Defines how the attribute is called where ZITADEL can get the id of the  user.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr

IdentityProviderServiceOAuthConfig.model_rebuild(raise_errors=False)
