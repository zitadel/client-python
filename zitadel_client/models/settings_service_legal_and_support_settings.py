# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class SettingsServiceLegalAndSupportSettings(BaseModel):
    """
    SettingsServiceLegalAndSupportSettings
    """

    tos_link: Optional[StrictStr] = Field(
        default=None,
        alias="tosLink",
        description="Link to the Terms of Service. Can be a relative or absolute URL.",
    )
    privacy_policy_link: Optional[StrictStr] = Field(
        default=None,
        alias="privacyPolicyLink",
        description="Link to the Privacy Policy. Can be a relative or absolute URL.",
    )
    help_link: Optional[StrictStr] = Field(
        default=None,
        alias="helpLink",
        description="Link to a help page. Can be a relative or absolute URL.",
    )
    support_email: Optional[StrictStr] = Field(
        default=None,
        alias="supportEmail",
        description="Email address for support issues.",
    )
    resource_owner_type: Optional[SettingsServiceResourceOwnerType] = Field(
        default=None, alias="resourceOwnerType"
    )
    docs_link: Optional[StrictStr] = Field(
        default=None,
        alias="docsLink",
        description="Link to documentation to be shown in the console.",
    )
    custom_link: Optional[StrictStr] = Field(
        default=None,
        alias="customLink",
        description="Link to an external resource that will be available to users in the console.",
    )
    custom_link_text: Optional[StrictStr] = Field(
        default=None,
        alias="customLinkText",
        description="The button text that would be shown in console pointing to custom link.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.settings_service_resource_owner_type import (
    SettingsServiceResourceOwnerType,
)

SettingsServiceLegalAndSupportSettings.model_rebuild(raise_errors=False)
