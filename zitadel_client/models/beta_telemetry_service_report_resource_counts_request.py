# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class BetaTelemetryServiceReportResourceCountsRequest(BaseModel):
    """
    BetaTelemetryServiceReportResourceCountsRequest
    """

    system_id: Optional[StrictStr] = Field(
        default=None,
        alias="systemId",
        description="The system ID is a unique identifier for the ZITADEL system.",
    )
    report_id: Optional[StrictStr] = Field(
        default=None,
        alias="reportId",
        description="The previously returned report ID from the server to continue reporting.  Note that the report ID is only valid for the same system ID.",
    )
    resource_counts: Optional[List[BetaTelemetryServiceResourceCount]] = Field(
        default=None,
        alias="resourceCounts",
        description="A list of resource counts to report.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.beta_telemetry_service_resource_count import (
    BetaTelemetryServiceResourceCount,
)

BetaTelemetryServiceReportResourceCountsRequest.model_rebuild(raise_errors=False)
