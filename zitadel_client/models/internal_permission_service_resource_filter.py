# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class InternalPermissionServiceResourceFilter(BaseModel):
    """
    InternalPermissionServiceResourceFilter
    """

    instance: Optional[StrictBool] = Field(
        default=None,
        alias="instance",
        description="Search for administrators granted on the instance level.",
    )
    organization_id: Optional[StrictStr] = Field(
        default=None,
        alias="organizationId",
        description="Search for administrators granted on a specific organization.",
    )
    project_grant: Optional[InternalPermissionServiceProjectGrant] = Field(
        default=None, alias="projectGrant"
    )
    project_id: Optional[StrictStr] = Field(
        default=None,
        alias="projectId",
        description="Search for administrators granted on a specific project.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from pydantic import StrictStr
from zitadel_client.models.internal_permission_service_project_grant import (
    InternalPermissionServiceProjectGrant,
)

InternalPermissionServiceResourceFilter.model_rebuild(raise_errors=False)
