# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class InstanceServiceListTrustedDomainsRequest(BaseModel):
    """
    InstanceServiceListTrustedDomainsRequest
    """

    instance_id: Optional[StrictStr] = Field(
        default=None,
        alias="instanceId",
        description="InstanceID is the unique ID of the instance whose trusted domains will be listed.  If not set, the instance in the current context (e.g. identified by the host header) will be used.  If an ID is set, the caller must have additional permissions.",
    )
    pagination: Optional[InstanceServicePaginationRequest] = Field(
        default=None, alias="pagination"
    )
    sorting_column: Optional[InstanceServiceTrustedDomainFieldName] = Field(
        default=None, alias="sortingColumn"
    )
    filters: Optional[List[InstanceServiceTrustedDomainFilter]] = Field(
        default=None, alias="filters", description="Filter the domains to be returned."
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.instance_service_pagination_request import (
    InstanceServicePaginationRequest,
)
from zitadel_client.models.instance_service_trusted_domain_field_name import (
    InstanceServiceTrustedDomainFieldName,
)
from zitadel_client.models.instance_service_trusted_domain_filter import (
    InstanceServiceTrustedDomainFilter,
)

InstanceServiceListTrustedDomainsRequest.model_rebuild(raise_errors=False)
