# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class BetaProjectServiceCreateProjectRequest(BaseModel):
    """
    BetaProjectServiceCreateProjectRequest
    """

    organization_id: Optional[StrictStr] = Field(
        default=None,
        alias="organizationId",
        description="The unique identifier of the organization the project belongs to.",
    )
    id: Optional[StrictStr] = Field(
        default=None, alias="id", description="The unique identifier of the project."
    )
    name: Optional[StrictStr] = Field(
        default=None, alias="name", description="Name of the project."
    )
    project_role_assertion: Optional[StrictBool] = Field(
        default=None,
        alias="projectRoleAssertion",
        description="Enable this setting to provide role information to your application. For OpenID Connect, the roles can be requested from the UserInfo endpoint or sent in the ID or Access Token, based on your application's configuration.",
    )
    authorization_required: Optional[StrictBool] = Field(
        default=None,
        alias="authorizationRequired",
        description="Deny authentication if the user has no authorization assigned to this project. Authorizations to the project without assigned a specific role to the user are allowed.",
    )
    project_access_required: Optional[StrictBool] = Field(
        default=None,
        alias="projectAccessRequired",
        description="Before a user can be authenticated, it is verified that their affiliated organization has been granted access to this project. Authentication is not permitted for users from unauthorized organizations.",
    )
    private_labeling_setting: Optional[BetaProjectServicePrivateLabelingSetting] = (
        Field(default=None, alias="privateLabelingSetting")
    )
    admins: Optional[List[BetaProjectServiceAdmin]] = Field(
        default=None,
        alias="admins",
        description="List of users and Project Member roles (PROJECT_OWNER, by default) to be assigned to those users.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from pydantic import StrictStr
from zitadel_client.models.beta_project_service_admin import BetaProjectServiceAdmin
from zitadel_client.models.beta_project_service_private_labeling_setting import (
    BetaProjectServicePrivateLabelingSetting,
)

BetaProjectServiceCreateProjectRequest.model_rebuild(raise_errors=False)
