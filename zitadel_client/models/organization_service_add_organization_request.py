# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class OrganizationServiceAddOrganizationRequest(BaseModel):
    """
    OrganizationServiceAddOrganizationRequest
    """

    name: Optional[StrictStr] = Field(
        default=None,
        alias="name",
        description="Name is the unique name of the organization to be created.  This must be unique across the instance.",
    )
    admins: Optional[List[OrganizationServiceAdmin]] = Field(
        default=None,
        alias="admins",
        description="Specify users to be assigned as organization admins.  If no users are specified here, the organization will be created without any admin users.  The organization can still be managed by any instance administrator.  If no roles are specified for a user, they will be assigned the role ORG_OWNER.",
    )
    organization_id: Optional[StrictStr] = Field(
        default=None,
        alias="organizationId",
        description="OrganizationID is the unique identifier of the organization. This field is optional.  If omitted, the system will generate one,  which is the recommended way. The generated ID will be returned in the response.",
    )
    # .. deprecated:: This property is deprecated.
    org_id: Optional[StrictStr] = Field(
        default=None,
        alias="orgId",
        description="Optionally, set a unique id for the organization. If omitted, the system will generate one,  which is the recommended way. The generated ID will be returned in the response.   Deprecated: use 'organization_id' field instead.  If both org_id and organization_id are set, organization_id will take precedence.",
    )

    @field_validator("org_id")
    def org_id_warn_deprecated(cls, value: Any) -> Any:
        """Emits a DeprecationWarning when the deprecated field `org_id` is set."""
        if value is not None:
            warnings.warn(
                "field 'org_id' on OrganizationServiceAddOrganizationRequest is deprecated",
                DeprecationWarning,
                stacklevel=2,
            )
        return value

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.organization_service_admin import OrganizationServiceAdmin

OrganizationServiceAddOrganizationRequest.model_rebuild(raise_errors=False)
