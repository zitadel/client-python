# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class OrganizationServiceConnectErrorCodeEnum(str, Enum):
    """OrganizationServiceConnectError - code"""

    CANCELED = "canceled"
    UNKNOWN = "unknown"
    INVALID_ARGUMENT = "invalid_argument"
    DEADLINE_EXCEEDED = "deadline_exceeded"
    NOT_FOUND = "not_found"
    ALREADY_EXISTS = "already_exists"
    PERMISSION_DENIED = "permission_denied"
    RESOURCE_EXHAUSTED = "resource_exhausted"
    FAILED_PRECONDITION = "failed_precondition"
    ABORTED = "aborted"
    OUT_OF_RANGE = "out_of_range"
    UNIMPLEMENTED = "unimplemented"
    INTERNAL = "internal"
    UNAVAILABLE = "unavailable"
    DATA_LOSS = "data_loss"
    UNAUTHENTICATED = "unauthenticated"


class OrganizationServiceConnectError(BaseModel):
    """
    Error type returned by Connect: https://connectrpc.com/docs/go/errors/#http-representation
    """

    code: Optional[OrganizationServiceConnectErrorCodeEnum] = Field(
        default=None,
        alias="code",
        description="The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].",
    )
    message: Optional[StrictStr] = Field(
        default=None,
        alias="message",
        description="A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.",
    )
    details: Optional[List[OrganizationServiceAny]] = Field(
        default=None,
        alias="details",
        description="A list of messages that carry the error details. There is no limit on the number of messages.",
    )
    additional_properties: Dict[str, Any] = Field(default_factory=dict)

    @model_validator(mode="before")
    @classmethod
    def _capture_additional_properties(cls, values: Any) -> Any:
        """Collect JSON keys not declared as fields into additional_properties.

        Pydantic v2 doesn't auto-merge unknown keys into a typed dict
        field. Here we walk the incoming mapping, split known-vs-extra
        based on the model's declared fields (by alias and by name), and
        funnel extras into `additional_properties`. This makes the
        Metadata-style schema (fixed fields + additionalProperties: {...})
        round-trip without dropping data, matching the cross-language
        contract.
        """
        if not isinstance(values, dict):
            return values
        known: Set[str] = set()
        for fname, finfo in cls.model_fields.items():
            known.add(fname)
            if finfo.alias is not None:
                known.add(finfo.alias)
        extras: Dict[str, Any] = values.get("additional_properties") or {}
        if not isinstance(extras, dict):
            extras = {}
        merged: Dict[str, Any] = {}
        for key, value in values.items():
            if key == "additional_properties":
                continue
            if key in known:
                merged[key] = value
            else:
                extras[key] = value
        merged["additional_properties"] = extras
        return merged

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.organization_service_any import OrganizationServiceAny

OrganizationServiceConnectError.model_rebuild(raise_errors=False)
