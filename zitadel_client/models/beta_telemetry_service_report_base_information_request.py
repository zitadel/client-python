# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class BetaTelemetryServiceReportBaseInformationRequest(BaseModel):
    """
    BetaTelemetryServiceReportBaseInformationRequest
    """

    system_id: Optional[StrictStr] = Field(
        default=None,
        alias="systemId",
        description="The system ID is a unique identifier for the ZITADEL system.",
    )
    version: Optional[StrictStr] = Field(
        default=None,
        alias="version",
        description="The current version of the ZITADEL system.",
    )
    instances: Optional[List[BetaTelemetryServiceInstanceInformation]] = Field(
        default=None,
        alias="instances",
        description="A list of instances in the ZITADEL system and their information.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.beta_telemetry_service_instance_information import (
    BetaTelemetryServiceInstanceInformation,
)

BetaTelemetryServiceReportBaseInformationRequest.model_rebuild(raise_errors=False)
