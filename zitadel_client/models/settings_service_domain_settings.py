# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class SettingsServiceDomainSettings(BaseModel):
    """
    SettingsServiceDomainSettings
    """

    login_name_includes_domain: Optional[StrictBool] = Field(
        default=None,
        alias="loginNameIncludesDomain",
        description="If enabled, the login name will automatically be suffixed with the domain of the organization.  This ensures that the login name is unique across the instance.",
    )
    require_org_domain_verification: Optional[StrictBool] = Field(
        default=None,
        alias="requireOrgDomainVerification",
        description="If enabled, organization domains must be verified (through an DNS or HTTP challenge) upon creation.  If disabled, organization domains will be created as already verified automatically.",
    )
    smtp_sender_address_matches_instance_domain: Optional[StrictBool] = Field(
        default=None,
        alias="smtpSenderAddressMatchesInstanceDomain",
        description="If enabled, the SMTP sender address domain must match custom domain on the instance.",
    )
    resource_owner_type: Optional[SettingsServiceResourceOwnerType] = Field(
        default=None, alias="resourceOwnerType"
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from zitadel_client.models.settings_service_resource_owner_type import (
    SettingsServiceResourceOwnerType,
)

SettingsServiceDomainSettings.model_rebuild(raise_errors=False)
