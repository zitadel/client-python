# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class InternalPermissionServiceUser(BaseModel):
    """
    InternalPermissionServiceUser
    """

    id: Optional[StrictStr] = Field(
        default=None, alias="id", description="ID is the unique identifier of the user."
    )
    preferred_login_name: Optional[StrictStr] = Field(
        default=None,
        alias="preferredLoginName",
        description="PreferredLoginName is the preferred login name of the user. This value is unique across the whole instance.",
    )
    display_name: Optional[StrictStr] = Field(
        default=None,
        alias="displayName",
        description="DisplayName is the public display name of the user.  By default it's the user's given name and family name, their username or their email address.",
    )
    organization_id: Optional[StrictStr] = Field(
        default=None,
        alias="organizationId",
        description="The organization the user belong to.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr

InternalPermissionServiceUser.model_rebuild(raise_errors=False)
