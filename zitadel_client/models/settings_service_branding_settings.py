# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class SettingsServiceBrandingSettings(BaseModel):
    """
    SettingsServiceBrandingSettings
    """

    light_theme: Optional[SettingsServiceTheme] = Field(
        default=None, alias="lightTheme"
    )
    dark_theme: Optional[SettingsServiceTheme] = Field(default=None, alias="darkTheme")
    font_url: Optional[StrictStr] = Field(
        default=None, alias="fontUrl", description="The url where the font is served."
    )
    hide_login_name_suffix: Optional[StrictBool] = Field(
        default=None,
        alias="hideLoginNameSuffix",
        description='If enabled, the organization suffix will be hidden on the login form if the scope \\"urn:zitadel:iam:org:domain:primary:{domainname}\\" is used.',
    )
    disable_watermark: Optional[StrictBool] = Field(
        default=None,
        alias="disableWatermark",
        description="If enabled, the Zitadel logo will not be displayed on the login screen.",
    )
    resource_owner_type: Optional[SettingsServiceResourceOwnerType] = Field(
        default=None, alias="resourceOwnerType"
    )
    theme_mode: Optional[SettingsServiceThemeMode] = Field(
        default=None, alias="themeMode"
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from pydantic import StrictStr
from zitadel_client.models.settings_service_resource_owner_type import (
    SettingsServiceResourceOwnerType,
)
from zitadel_client.models.settings_service_theme import SettingsServiceTheme
from zitadel_client.models.settings_service_theme_mode import SettingsServiceThemeMode

SettingsServiceBrandingSettings.model_rebuild(raise_errors=False)
