# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class BetaInternalPermissionServiceCreateAdministratorRequest(BaseModel):
    """
    BetaInternalPermissionServiceCreateAdministratorRequest
    """

    user_id: Optional[StrictStr] = Field(
        default=None,
        alias="userId",
        description="UserID is the ID of the user who should be granted the administrator role.",
    )
    resource: Optional[BetaInternalPermissionServiceResourceType] = Field(
        default=None, alias="resource"
    )
    roles: Optional[List[StrictStr]] = Field(
        default=None,
        alias="roles",
        description="Roles are the roles that should be granted to the user for the specified resource.  Note that roles are currently specific to the resource type.  This means that if you want to grant a user the administrator role for an organization and a project,  you need to create two administrator roles.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.beta_internal_permission_service_resource_type import (
    BetaInternalPermissionServiceResourceType,
)

BetaInternalPermissionServiceCreateAdministratorRequest.model_rebuild(
    raise_errors=False
)
