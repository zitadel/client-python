# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field
from pydantic import StrictStr
from typing import Optional


class ProjectServiceAddProjectRoleRequest(BaseModel):
    """
    ProjectServiceAddProjectRoleRequest
    """

    project_id: Optional[StrictStr] = Field(
        default=None,
        alias="projectId",
        description="ProjectID is the unique identifier of the project.",
    )
    role_key: Optional[StrictStr] = Field(
        default=None,
        alias="roleKey",
        description="RoleKey identifies the role. It's the only relevant attribute for ZITADEL and  will be used for authorization checks and as claim in tokens and user info responses.",
    )
    display_name: Optional[StrictStr] = Field(
        default=None,
        alias="displayName",
        description="DisplayName is a human readable name for the role, which might be displayed to users.",
    )
    group: Optional[StrictStr] = Field(
        default=None,
        alias="group",
        description="Group allows grouping roles for display purposes. Zitadel will not handle it in any way.  It can be used to group roles in a UI to allow easier management for administrators.  This attribute is not to be confused with groups as a collection of users.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


ProjectServiceAddProjectRoleRequest.model_rebuild(raise_errors=False)
