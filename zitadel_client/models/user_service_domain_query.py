# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field
from pydantic import StrictBool, StrictStr
from typing import Optional


class UserServiceDomainQuery(BaseModel):
    """
    UserServiceDomainQuery
    """

    include_without_domain: Optional[StrictBool] = Field(
        default=None,
        alias="includeWithoutDomain",
        description="List also auth method types without domain information like passkey and U2F added through V1 APIs / Login UI.",
    )
    domain: Optional[StrictStr] = Field(
        default=None,
        alias="domain",
        description="List only auth methods with specific domain.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


UserServiceDomainQuery.model_rebuild(raise_errors=False)
