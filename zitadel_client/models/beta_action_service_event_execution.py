# coding: utf-8

"""
    Zitadel SDK

    The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from zitadel_client.models.all import All
from zitadel_client.models.event1 import Event1
from zitadel_client.models.group import Group
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

none_type = type(None)
BETAACTIONSERVICEEVENTEXECUTION_ONE_OF_SCHEMAS = ["All", "Event1", "Group"]

class BetaActionServiceEventExecution(BaseModel):
    """
    BetaActionServiceEventExecution
    """
    # data type: All
    oneof_schema_1_validator: Optional[All] = None
    # data type: Event1
    oneof_schema_2_validator: Optional[Event1] = None
    # data type: Group
    oneof_schema_3_validator: Optional[Group] = None
    actual_instance: Optional[Union[All, Event1, Group]] = None
    one_of_schemas: Set[str] = { "All", "Event1", "Group" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = BetaActionServiceEventExecution.model_construct()
        error_messages = []
        match = 0
        # validate data type: All
        if not isinstance(v, All):
            error_messages.append(f"Error! Input type `{type(v)}` is not `All`")
        else:
            match += 1
        # validate data type: Event1
        if not isinstance(v, Event1):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Event1`")
        else:
            match += 1
        # validate data type: Group
        if not isinstance(v, Group):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Group`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in BetaActionServiceEventExecution with oneOf schemas: All, Event1, Group. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in BetaActionServiceEventExecution with oneOf schemas: All, Event1, Group. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into All
        try:
            instance.actual_instance = All.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Event1
        try:
            instance.actual_instance = Event1.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Group
        try:
            instance.actual_instance = Group.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into BetaActionServiceEventExecution with oneOf schemas: All, Event1, Group. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into BetaActionServiceEventExecution with oneOf schemas: All, Event1, Group. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], All, Event1, Group]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


