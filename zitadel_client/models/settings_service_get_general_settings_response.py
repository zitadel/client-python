# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class SettingsServiceGetGeneralSettingsResponse(BaseModel):
    """
    SettingsServiceGetGeneralSettingsResponse
    """

    default_org_id: Optional[StrictStr] = Field(
        default=None,
        alias="defaultOrgId",
        description="The unique identifier of the default organization.  The default organization is used to assign new users to an organization if no other organization is specified.  Deprecated: use default_organization_id instead.",
    )
    default_language: Optional[StrictStr] = Field(
        default=None,
        alias="defaultLanguage",
        description='The default language is use if no other language is specified or detected.  The format is a BCP 47 language tag (e.g. "en", "de", "fr-CH").',
    )
    supported_languages: Optional[List[StrictStr]] = Field(
        default=None,
        alias="supportedLanguages",
        description='The list of supported languages.  Note that the instance might restrict the languages further  only allowing a subset of these languages to be used.  The format is a BCP 47 language tag (e.g. "en", "de", "fr-CH").',
    )
    default_organization_id: Optional[StrictStr] = Field(
        default=None,
        alias="defaultOrganizationId",
        description="The unique identifier of the default organization.  The default organization is used to assign new users to an organization if no other organization is specified.",
    )
    allowed_languages: Optional[List[StrictStr]] = Field(
        default=None,
        alias="allowedLanguages",
        description='The list of allowed languages for the instance.  This is a subset of the supported languages to be used in the instance  e.g. for user selection during registration or language detection in the UI.  The format is a BCP 47 language tag (e.g. "en", "de", "fr-CH").',
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr

SettingsServiceGetGeneralSettingsResponse.model_rebuild(raise_errors=False)
