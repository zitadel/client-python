# coding: utf-8

"""
    Zitadel SDK

    The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from zitadel_client.models.rest_async import RestAsync
from zitadel_client.models.rest_call import RestCall
from zitadel_client.models.rest_webhook import RestWebhook
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

BETAACTIONSERVICETARGET_ONE_OF_SCHEMAS = ["RestAsync", "RestCall", "RestWebhook"]

class BetaActionServiceTarget(BaseModel):
    """
    BetaActionServiceTarget
    """
    # data type: RestAsync
    oneof_schema_1_validator: Optional[RestAsync] = None
    # data type: RestCall
    oneof_schema_2_validator: Optional[RestCall] = None
    # data type: RestWebhook
    oneof_schema_3_validator: Optional[RestWebhook] = None
    actual_instance: Optional[Union[RestAsync, RestCall, RestWebhook]] = None
    one_of_schemas: Set[str] = { "RestAsync", "RestCall", "RestWebhook" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = BetaActionServiceTarget.model_construct()
        error_messages = []
        match = 0
        # validate data type: RestAsync
        if not isinstance(v, RestAsync):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RestAsync`")
        else:
            match += 1
        # validate data type: RestCall
        if not isinstance(v, RestCall):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RestCall`")
        else:
            match += 1
        # validate data type: RestWebhook
        if not isinstance(v, RestWebhook):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RestWebhook`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in BetaActionServiceTarget with oneOf schemas: RestAsync, RestCall, RestWebhook. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in BetaActionServiceTarget with oneOf schemas: RestAsync, RestCall, RestWebhook. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into RestAsync
        try:
            instance.actual_instance = RestAsync.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RestCall
        try:
            instance.actual_instance = RestCall.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RestWebhook
        try:
            instance.actual_instance = RestWebhook.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into BetaActionServiceTarget with oneOf schemas: RestAsync, RestCall, RestWebhook. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into BetaActionServiceTarget with oneOf schemas: RestAsync, RestCall, RestWebhook. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], RestAsync, RestCall, RestWebhook]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


