# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field
from pydantic import StrictStr
from typing import Optional


class OrganizationServiceUpdateOrganizationRequest(BaseModel):
    """
    OrganizationServiceUpdateOrganizationRequest
    """

    organization_id: Optional[StrictStr] = Field(
        default=None,
        alias="organizationId",
        description="OrganizationID is the unique identifier of the organization to be updated.",
    )
    name: Optional[StrictStr] = Field(
        default=None,
        alias="name",
        description="Name is the new name for the organization to be set.  Note that since the name is used to generate the organization's default domain,  changing the name will also change the domain.  Additionally, if the domain is used as suffix for user logins,  their login names will also change accordingly.  It will not affect any custom domains added to the organization.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


OrganizationServiceUpdateOrganizationRequest.model_rebuild(raise_errors=False)
