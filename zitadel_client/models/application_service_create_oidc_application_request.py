# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class ApplicationServiceCreateOIDCApplicationRequest(BaseModel):
    """
    ApplicationServiceCreateOIDCApplicationRequest
    """

    redirect_uris: Optional[List[StrictStr]] = Field(
        default=None,
        alias="redirectUris",
        description="RedirectURIs are the allowed callback URIs for the OAuth2 / OIDC flows,  where the authorization code or tokens will be sent to.  The redirect_uri parameter in the authorization request must exactly match one of these URIs.",
    )
    response_types: Optional[List[ApplicationServiceOIDCResponseType]] = Field(
        default=None,
        alias="responseTypes",
        description="ResponseTypes define whether a code, id_token token or just id_token will be returned.  The response_type parameter in the authorization request must exactly match one of these values.",
    )
    grant_types: Optional[List[ApplicationServiceOIDCGrantType]] = Field(
        default=None,
        alias="grantTypes",
        description="GrantTypes define the flow type the application is allowed to use.  The grant_type parameter in the token request must exactly match one of these values.  Minimum one grant type must be provided, but multiple grant types can be provided to allow  different flows, e.g. authorization code flow and refresh token flow.",
    )
    application_type: Optional[ApplicationServiceOIDCApplicationType] = Field(
        default=None, alias="applicationType"
    )
    auth_method_type: Optional[ApplicationServiceOIDCAuthMethodType] = Field(
        default=None, alias="authMethodType"
    )
    post_logout_redirect_uris: Optional[List[StrictStr]] = Field(
        default=None,
        alias="postLogoutRedirectUris",
        description="PostLogoutRedirectURIs are the allowed URIs to redirect to after a logout.  The post_logout_redirect_uri parameter in the logout request must exactly match one of these URIs.",
    )
    version: Optional[ApplicationServiceOIDCVersion] = Field(
        default=None, alias="version"
    )
    development_mode: Optional[StrictBool] = Field(
        default=None,
        alias="developmentMode",
        description="DevelopmentMode can be enabled for development purposes. This allows the use of  OIDC non-compliant and potentially insecure settings, such as the use of  HTTP redirect URIs or wildcard redirect URIs.",
    )
    access_token_type: Optional[ApplicationServiceOIDCTokenType] = Field(
        default=None, alias="accessTokenType"
    )
    access_token_role_assertion: Optional[StrictBool] = Field(
        default=None,
        alias="accessTokenRoleAssertion",
        description="If AccessTokenRoleAssertion is enabled, the roles of the user are added to the access token.  Ensure that the access token is a JWT token and not a bearer token. And either request the roles  by scope or enable the user role assertion on the project.",
    )
    id_token_role_assertion: Optional[StrictBool] = Field(
        default=None,
        alias="idTokenRoleAssertion",
        description="If IDTokenRoleAssertion is enabled, the roles of the user are added to the id token.  Ensure that either the roles are requested by scope or enable the user role assertion on the  project.",
    )
    id_token_userinfo_assertion: Optional[StrictBool] = Field(
        default=None,
        alias="idTokenUserinfoAssertion",
        description="If IDTokenUserinfoAssertion is enabled, the claims of profile, email, address and phone scopes  are added to the id token even if an access token is issued. This can be required by some applications  that do not call the userinfo endpoint after authentication or directly use the id_token for retrieving  user information.  Attention: this violates the OIDC specification, which states that these claims must only be  requested from the userinfo endpoint if an access token is issued. This is to prevent  leaking of personal information in the id token, which is often stored in the browser and  therefore more vulnerable.",
    )
    clock_skew: Optional[ProtobufDuration] = Field(
        default=None,
        alias="clockSkew",
        description='A Duration represents a signed, fixed-length span of time represented  as a count of seconds and fractions of seconds at nanosecond  resolution. It is independent of any calendar and concepts like "day"  or "month". It is related to Timestamp in that the difference between  two Timestamp values is a Duration and it can be added or subtracted  from a Timestamp. Range is approximately +-10,000 years.   # Examples   Example 1: Compute Duration from two Timestamps in pseudo code.       Timestamp start = ...;      Timestamp end = ...;      Duration duration = ...;       duration.seconds = end.seconds - start.seconds;      duration.nanos = end.nanos - start.nanos;       if (duration.seconds < 0 && duration.nanos > 0) {        duration.seconds += 1;        duration.nanos -= 1000000000;      } else if (duration.seconds > 0 && duration.nanos < 0) {        duration.seconds -= 1;        duration.nanos += 1000000000;      }   Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.       Timestamp start = ...;      Duration duration = ...;      Timestamp end = ...;       end.seconds = start.seconds + duration.seconds;      end.nanos = start.nanos + duration.nanos;       if (end.nanos < 0) {        end.seconds -= 1;        end.nanos += 1000000000;      } else if (end.nanos >= 1000000000) {        end.seconds += 1;        end.nanos -= 1000000000;      }   Example 3: Compute Duration from datetime.timedelta in Python.       td = datetime.timedelta(days=3, minutes=10)      duration = Duration()      duration.FromTimedelta(td)   # JSON Mapping   In JSON format, the Duration type is encoded as a string rather than an  object, where the string ends in the suffix "s" (indicating seconds) and  is preceded by the number of seconds, with nanoseconds expressed as  fractional seconds. For example, 3 seconds with 0 nanoseconds should be  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should  be expressed in JSON format as "3.000000001s", and 3 seconds and 1  microsecond should be expressed in JSON format as "3.000001s".',
    )
    additional_origins: Optional[List[StrictStr]] = Field(
        default=None,
        alias="additionalOrigins",
        description="AdditionalOrigins are HTTP origins (scheme + host + port) from where the API can be used  additional to the redirect_uris.  This is useful if the application is used from an origin different to the redirect_uris,  e.g. if the application is a SPA served in a native app, where the redirect_uri is a custom scheme,  but the application is served from a https origin.",
    )
    skip_native_app_success_page: Optional[StrictBool] = Field(
        default=None,
        alias="skipNativeAppSuccessPage",
        description="For native apps a successful login usually shows a success page with a link to open the application again.  SkipNativeAppSuccessPage can be used to skip this page and open the application directly.",
    )
    back_channel_logout_uri: Optional[StrictStr] = Field(
        default=None,
        alias="backChannelLogoutUri",
        description="BackChannelLogoutURI is used to notify the application about terminated sessions according  to the OIDC Back-Channel Logout (https://openid.net/specs/openid-connect-backchannel-1_0.html).",
    )
    login_version: Optional[ApplicationServiceLoginVersion] = Field(
        default=None, alias="loginVersion"
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from pydantic import StrictStr
from zitadel_client._duration import ProtobufDuration
from zitadel_client.models.application_service_login_version import (
    ApplicationServiceLoginVersion,
)
from zitadel_client.models.application_service_oidc_application_type import (
    ApplicationServiceOIDCApplicationType,
)
from zitadel_client.models.application_service_oidc_auth_method_type import (
    ApplicationServiceOIDCAuthMethodType,
)
from zitadel_client.models.application_service_oidc_grant_type import (
    ApplicationServiceOIDCGrantType,
)
from zitadel_client.models.application_service_oidc_response_type import (
    ApplicationServiceOIDCResponseType,
)
from zitadel_client.models.application_service_oidc_token_type import (
    ApplicationServiceOIDCTokenType,
)
from zitadel_client.models.application_service_oidc_version import (
    ApplicationServiceOIDCVersion,
)

ApplicationServiceCreateOIDCApplicationRequest.model_rebuild(raise_errors=False)
