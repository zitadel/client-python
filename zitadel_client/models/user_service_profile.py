# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class UserServiceProfile(BaseModel):
    """
    UserServiceProfile
    """

    given_name: Optional[StrictStr] = Field(
        default=None,
        alias="givenName",
        description="The given name is the first name of the user.  For example, it can be used to personalize notifications and login UIs.",
    )
    family_name: Optional[StrictStr] = Field(
        default=None,
        alias="familyName",
        description="The family name is the last name of the user.  For example, it can be used to personalize user interfaces and notifications.",
    )
    nick_name: Optional[StrictStr] = Field(
        default=None,
        alias="nickName",
        description="The nick name is the users short name.  For example, it can be used to personalize user interfaces and notifications.",
    )
    display_name: Optional[StrictStr] = Field(
        default=None,
        alias="displayName",
        description="The display name is how a user should primarily be displayed in lists.  It can also for example be used to personalize user interfaces and notifications.",
    )
    preferred_language: Optional[StrictStr] = Field(
        default=None,
        alias="preferredLanguage",
        description="The users preferred language is the language that systems should use to interact with the user.  It has the format of a [BCP-47 language tag](https://datatracker.ietf.org/doc/html/rfc3066).  It is used by Zitadel where no higher prioritized preferred language can be used.  For example, browser settings can overwrite a users preferred_language.  Notification messages and standard login UIs use the users preferred language if it is supported and allowed on the instance.  Else, the default language of the instance is used.",
    )
    gender: Optional[UserServiceGender] = Field(default=None, alias="gender")

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.user_service_gender import UserServiceGender

UserServiceProfile.model_rebuild(raise_errors=False)
