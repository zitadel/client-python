# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class UserServiceUpdateUserRequest(BaseModel):
    """
    UserServiceUpdateUserRequest
    """

    user_id: Optional[StrictStr] = Field(
        default=None,
        alias="userId",
        description="The user id is the users unique identifier in the instance.  It can't be changed.",
    )
    username: Optional[StrictStr] = Field(
        default=None,
        alias="username",
        description="Set a new username that is unique within the instance.  Beware that active tokens and sessions are invalidated when the username is changed.",
    )
    human: Optional[UserServiceHuman] = Field(default=None, alias="human")
    machine: Optional[UserServiceMachine] = Field(default=None, alias="machine")

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr
from zitadel_client.models.user_service_human import UserServiceHuman
from zitadel_client.models.user_service_machine import UserServiceMachine

UserServiceUpdateUserRequest.model_rebuild(raise_errors=False)
