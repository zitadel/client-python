# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class BetaProjectServiceUpdateProjectRequest(BaseModel):
    """
    BetaProjectServiceUpdateProjectRequest
    """

    id: Optional[StrictStr] = Field(default=None, alias="id")
    name: Optional[StrictStr] = Field(
        default=None, alias="name", description="Name of the project."
    )
    project_role_assertion: Optional[StrictBool] = Field(
        default=None,
        alias="projectRoleAssertion",
        description="Enable this setting to have role information included in the user info endpoint. It is also dependent on your application settings to include it in tokens and other types.",
    )
    project_role_check: Optional[StrictBool] = Field(
        default=None,
        alias="projectRoleCheck",
        description="When enabled ZITADEL will check if a user has a role of this project assigned when login into an application of this project.",
    )
    has_project_check: Optional[StrictBool] = Field(
        default=None,
        alias="hasProjectCheck",
        description="When enabled ZITADEL will check if the organization of the user, that is trying to log in, has a grant to this project.",
    )
    private_labeling_setting: Optional[BetaProjectServicePrivateLabelingSetting] = (
        Field(default=None, alias="privateLabelingSetting")
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from pydantic import StrictStr
from zitadel_client.models.beta_project_service_private_labeling_setting import (
    BetaProjectServicePrivateLabelingSetting,
)

BetaProjectServiceUpdateProjectRequest.model_rebuild(raise_errors=False)
