# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class IdentityProviderServiceSAMLConfig(BaseModel):
    """
    IdentityProviderServiceSAMLConfig
    """

    metadata_xml: Optional[bytes] = Field(
        default=None,
        alias="metadataXml",
        description="Metadata of the SAML identity provider.",
    )
    binding: Optional[IdentityProviderServiceSAMLBinding] = Field(
        default=None, alias="binding"
    )
    with_signed_request: Optional[StrictBool] = Field(
        default=None,
        alias="withSignedRequest",
        description="Boolean which defines if the authentication requests are signed.",
    )
    name_id_format: Optional[IdentityProviderServiceSAMLNameIDFormat] = Field(
        default=None, alias="nameIdFormat"
    )
    transient_mapping_attribute_name: Optional[StrictStr] = Field(
        default=None,
        alias="transientMappingAttributeName",
        description="Optional name of the attribute, which will be used to map the user  in case the nameid-format returned is  `urn:oasis:names:tc:SAML:2.0:nameid-format:transient`.",
    )
    federated_logout_enabled: Optional[StrictBool] = Field(
        default=None,
        alias="federatedLogoutEnabled",
        description="Boolean weather federated logout is enabled. If enabled, ZITADEL will send a logout request to the identity provider,  if the user terminates the session in ZITADEL. Be sure to provide a SLO endpoint as part of the metadata.",
    )
    signature_algorithm: Optional[IdentityProviderServiceSAMLSignatureAlgorithm] = (
        Field(default=None, alias="signatureAlgorithm")
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from pydantic import StrictStr
from zitadel_client.models.identity_provider_service_saml_binding import (
    IdentityProviderServiceSAMLBinding,
)
from zitadel_client.models.identity_provider_service_saml_name_id_format import (
    IdentityProviderServiceSAMLNameIDFormat,
)
from zitadel_client.models.identity_provider_service_saml_signature_algorithm import (
    IdentityProviderServiceSAMLSignatureAlgorithm,
)

IdentityProviderServiceSAMLConfig.model_rebuild(raise_errors=False)
