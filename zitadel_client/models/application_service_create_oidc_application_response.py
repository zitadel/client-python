# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class ApplicationServiceCreateOIDCApplicationResponse(BaseModel):
    """
    ApplicationServiceCreateOIDCApplicationResponse
    """

    client_id: Optional[StrictStr] = Field(
        default=None,
        alias="clientId",
        description="The unique OAuth2/OIDC client_id used for authentication of the application,  e.g. at the token endpoint.",
    )
    client_secret: Optional[StrictStr] = Field(
        default=None,
        alias="clientSecret",
        description="In case of using the OIDCAuthMethodType.OIDC_AUTH_METHOD_TYPE_CLIENT_SECRET_BASIC  or OIDCAuthMethodType.OIDC_AUTH_METHOD_TYPE_CLIENT_SECRET_POST the client_secret is generated and returned.  It must be stored safely, as it will not be possible to retrieve it again.  A new client_secret can be generated using the GenerateClientSecret endpoint.",
    )
    non_compliant: Optional[StrictBool] = Field(
        default=None,
        alias="nonCompliant",
        description="NonCompliant specifies whether the config is OIDC compliant. A production configuration SHOULD be compliant.  Non-compliant configurations can run into interoperability issues with OIDC libraries and tools.  Compliance problems are listed in the compliance_problems field.",
    )
    compliance_problems: Optional[List[ApplicationServiceOIDCLocalizedMessage]] = Field(
        default=None,
        alias="complianceProblems",
        description="ComplianceProblems lists the problems for non-compliant configurations.  In case of a compliant configuration, this list is empty.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictBool
from pydantic import StrictStr
from zitadel_client.models.application_service_oidc_localized_message import (
    ApplicationServiceOIDCLocalizedMessage,
)

ApplicationServiceCreateOIDCApplicationResponse.model_rebuild(raise_errors=False)
