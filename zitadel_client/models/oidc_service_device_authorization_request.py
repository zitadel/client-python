# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator
from typing import Any, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self


class OIDCServiceDeviceAuthorizationRequest(BaseModel):
    """
    OIDCServiceDeviceAuthorizationRequest
    """

    id: Optional[StrictStr] = Field(
        default=None,
        alias="id",
        description="The unique identifier of the device authorization request to be used for authorizing or denying the request.",
    )
    client_id: Optional[StrictStr] = Field(
        default=None,
        alias="clientId",
        description="The client_id of the application that initiated the device authorization request.",
    )
    scope: Optional[List[StrictStr]] = Field(
        default=None,
        alias="scope",
        description="The scopes requested by the application.",
    )
    app_name: Optional[StrictStr] = Field(
        default=None, alias="appName", description="Name of the client application."
    )
    project_name: Optional[StrictStr] = Field(
        default=None,
        alias="projectName",
        description="Name of the project the client application is part of.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import StrictStr

OIDCServiceDeviceAuthorizationRequest.model_rebuild(raise_errors=False)
