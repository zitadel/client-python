# ruff: noqa
# mypy: ignore-errors
# Zitadel SDK
# The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from abc import abstractmethod

from ..api_client import ApiClient
from .authenticator import Authenticator


class HttpAwareAuthenticator(Authenticator):
    """Extension of :class:`Authenticator` for authentication schemes that
    require making HTTP requests (e.g. OAuth2 token exchange, OpenID Connect
    discovery).

    Implementations receive a shared :class:`ApiClient` instance so that
    authentication-related HTTP calls (token endpoints, discovery documents)
    use the same transport configuration (proxy, TLS, timeouts) as regular
    API calls.

    The :class:`ApiClient` is injected by the :class:`Zitadel` class after
    construction, via :meth:`set_api_client`. Implementations must not make
    HTTP calls before the client is injected.

    Only OAuth2 and OpenID Connect authenticators implement this class.
    Simple authenticators (Basic, Bearer, API Key) do not need HTTP access
    and implement :class:`Authenticator` directly.
    """

    @abstractmethod
    def set_api_client(self, api_client: ApiClient) -> None:
        """Inject the shared API client for making HTTP requests.

        Called by the :class:`Zitadel` constructor after the :class:`ApiClient`
        has been created with the user's :class:`TransportOptions`.
        Implementations should store this reference and use it for all
        outbound HTTP calls (token exchange, discovery, etc.).

        Args:
            api_client: The shared API client instance.
        """
