[tool.poetry]
name = "zitadel"
version = "0.1.0"
description = ""
authors = []

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
python = ">=3.8"
